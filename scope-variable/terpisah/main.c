#include <stdio.h>
#include "header.h" // Sertakan header file yang berisi deklarasi konstanta

int main() {
    // Menggunakan konstanta PI yang didefinisikan di file constants.c
    printf("Nilai PI: %.2f\n", PI);
    printf("%s \n ", answer);
    return 0;
}
